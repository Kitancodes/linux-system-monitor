name: Bash CI

on:
   push: 
     branches: [ "main" ]
   pull_request:
     branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

     steps:
     - name: Chckout repository
       uses: actions/checkout@v3

     - name: Run Bash script
       run: bash hello.sh
